const showDates = (...datos) => {
    console.log(datos);
}

showDates('Isaac', 4, 'best@edu.co', 'colombia')

//por el parametro Rest podemos recibir todos los argumentos sin necesidad de escribirlos